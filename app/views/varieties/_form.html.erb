<%= bootstrap_form_with(model: variety, local: true) do |form| %>
  <% if variety.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(variety.errors.count, "error") %> prohibited this variety from being saved:</h2>

      <ul>
      <% variety.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.text_field :name, label: "Nombre" %>

  <%= form.text_field :scientific_name, label: "Nombre científico" %>

  <%= form.text_field :url %>

  <%= form.text_field :description, label: "Descripción" %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

<hr>
<%= bootstrap_form_with(model: [farm, plot, crop], local: true) do |form| %>
  <% if crop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crop.errors.count, "error") %> prohibited this crop from being saved:</h2>

      <ul>
      <% crop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :comment, label: "Comentario" %>

  <%= form.number_field :amount, label: "Cantidad" %>

  <%= form.select(:variety_id, varieties_for_select, :prompt => 'Seleccionar') %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

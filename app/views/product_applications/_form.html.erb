<%= bootstrap_form_with(model: [farm, plot, product_application], local: true) do |form| %>
  <% if product_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_application.errors.count, "error") %> prohibited this product_application from being saved:</h2>

      <ul>
      <% product_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= form.text_field :comment, label: "Comentario" %>

  <%= form.text_field :amount, label: "Cantidad" %>

  <%= form.select(:product_id, products_for_select, :prompt => 'Seleccionar') %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

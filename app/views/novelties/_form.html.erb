<%= bootstrap_form_with(model: novelty, local: true) do |form| %>
  <% if novelty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(novelty.errors.count, "error") %> prohibited this novelty from being saved:</h2>

      <ul>
        <% novelty.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title, label: "Título" %>
  <%= form.text_area :body, label: "Texto" %>
  
  <%= form.select(:product_id, products_for_select, :prompt => 'Seleccionar', :include_blank => true, label: "Producto") %>

  <%= form.select(:variety_id, varieties_for_select, :prompt => 'Seleccionar', :include_blank => true, label: "Variedad") %>

  <%= form.select(:type_problem_id, types_problems_for_select, :prompt => 'Seleccionar', :include_blank => true, label: "Tipo de problema") %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

<hr>
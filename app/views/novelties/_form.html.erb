<%= bootstrap_form_with(model: novelty, local: true) do |form| %>
  <% if novelty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(novelty.errors.count, "error") %> prohibited this novelty from being saved:</h2>

      <ul>
        <% novelty.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title, label: "Título" %>
  <%= form.text_area :body, label: "Texto" %>
  <%= form.collection_select :producer_ids, User.all, :id, :email, {:include_hidden => false}, {:multiple => true} %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

<hr>
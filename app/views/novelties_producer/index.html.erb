<p id="notice"><%= notice %></p>

<h1>novelties</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @novelties.each do |novelty| %>
      <tr>
        <td><%= novelty.title %></td>
        <td><%= novelty.user.email %></td>
        <td><%= link_to 'Show', novelties_producer_path(novelty) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

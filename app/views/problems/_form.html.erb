<%= bootstrap_form_with(model: [farm, plot, problem], local: true) do |form| %>
  <% if problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(problem.errors.count, "error") %> prohibited this problem from being saved:</h2>

      <ul>
      <% problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :comment, label: "Comentario" %>

  <%= form.hidden_field :user_id, :value => current_user.id %>

  <%= form.select(:type_problem_id, type_problems_for_select, :prompt => 'Select') %>
    
  <a onclick="showHide()">Otros (si no encuentra la opción en la lista, puede agregar un valor propio)</a>

  <%= render 'others/form', form: form %>
  
  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>

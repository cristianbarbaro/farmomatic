<%= bootstrap_form_with(model: type_problem, local: true) do |form| %>
  <% if type_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_problem.errors.count, "error") %> prohibited this type_problem from being saved:</h2>

      <ul>
      <% type_problem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :name %>

  <%= form.text_field :scientific_name %>

  <%= form.text_field :url %>

  <%= form.text_field :description %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<hr>
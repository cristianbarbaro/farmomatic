<%= bootstrap_form_with(model: [farm, plot, plantation], local: true) do |form| %>
  <% if plantation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plantation.errors.count, "error") %> prohibited this plantation from being saved:</h2>

      <ul>
      <% plantation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :comment, label: "Comentario" %>

  <%= form.number_field :amount, label: "Cantidad" %>

  <%= form.hidden_field :user_id, :value => current_user.id %>

  <%= form.select(:species_id, species_for_select, :prompt => 'Seleccionar') %>
  
  <a onclick="showHide()">Otros (si no encuentra la opción en la lista, puede agregar un valor propio)</a>

  <%= render 'others/form', form: form %>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>
